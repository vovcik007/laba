#include <stdio.h>

int main() {
    int edge;
    printf("Input edge length:");
    scanf("%d", &edge);
    int volume = edge * edge * edge;
    int area = edge * edge;
    printf(" \nThe volume is:%d", volume);
    printf(" \nThe area is:%d", area);
    return 0;
}
